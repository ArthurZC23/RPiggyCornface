local module = {}

module.encode = {}
module.decode = {}

module.encode.Skills = function(state)
    -- Hard to do case by case. Depend on data schema. A error here would be critical.
end

return module